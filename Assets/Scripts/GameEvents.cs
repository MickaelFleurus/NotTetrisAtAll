using System;

class GameEvents
{
    // public static event Action<EGameMode, int, int, EGameTimeLimit> OnGameStarted;
    // public static void InvokeGameStarted(EGameMode gameMode, int levelStart, int blockSize, EGameTimeLimit timeLimit) => OnGameStarted?.Invoke(gameMode, levelStart, blockSize, timeLimit);
}
